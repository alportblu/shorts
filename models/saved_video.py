class SavedVideo:
    def __init__(self, user_id, video_path, title, created_at):
        self.user_id = user_id
        self.video_path = video_path
        self.title = title
        self.created_at = created_at 